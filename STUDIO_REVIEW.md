# Studio module — Code review audit (BẢN GỘP v2 · 2026-09-07)

**Target:** `/home/anhtuan/DEV/TrillfaShop` · module `studio` (Laravel 11 + Vue 3 AI fashion image-generation studio)
**Bản chất file:** bản gộp thay báo cáo gốc 995 dòng (Phần A–L) — nhiệm vụ cũ đã merge theo các đợt vá hoàn thành (J/K/L + phiên song song). Chỉ giữ chi tiết cho việc **CÒN MỞ**; việc đã vá/đã loại nén thành bảng tra cứu.

> **Đọc nguyên bản khi cần bằng chứng từng dòng:**
> - Phần A–K.8: `git show 8817f85:STUDIO_REVIEW.md` (960 dòng, trong git).
> - Phần L (đợt vá production-refgen): chưa có trong git — đã gộp **đầy đủ** vào T1, §2, §6; backup nguyên bản gộp: `/tmp/STUDIO_REVIEW_pre-merge_2026-09-07.md` (tạm).
> Trạng thái từng mục dưới đây được kiểm chứng **trực tiếp trên working tree** (grep/read) lúc gộp, không dựa vào trí nhớ báo cáo cũ.

## PHẦN I — Studio UI/UX Redesign (2026-09-07, gộp 37 phiên UI-1 → UI-3aj)

> Track **GUI/UX/UI** chạy song song với audit code (ngoài phạm vi review). Spec gốc: `STUDIO_UI_REDESIGN.md` (PHẦN I phân tích + PHẦN II chrome). Provider: **CHỈ `deepseek-official`** (`deepseek-v4-pro` logic · `deepseek-v4-flash` cơ học) — workflow fan-out ≤6 agent, text contract, điều phối xác minh 100% bằng read/grep/build. Deploy: `scripts/deploy.sh` → SSH `ssh -p 65002 u310846799@145.79.25.57` (app `~/domains/trillfa.shop`).

**1. Chrome & layout** (UI-1/2/3): vá CSS production thiếu `ink-600/ink-950/cream-400` trong `@theme` · đồng bộ 1 hệ icon (StudioIcon 90+ Lucide) · thêm bộ class chrome `.seg/.seg-btn/.tool-btn/.icon-btn/.panel-head/.panel-title` · **right bar 1 cột** (`w-44→w-[115px]` theo đính chính người dùng) · header tool-btn/icon-btn · chips → `.seg` · mobile top bar + badge đếm · **mobile tối giản** (ContextToolbar desktop-only + floating `bottom-12` khi tool active; outputs drawer chỉ đóng bằng nút close; RegionTools luôn nổi) · **layer theo tỷ lệ khung hình** `addBlankLayer(bg,ratio)` + 7 preset tỷ lệ + màu tùy chỉnh + thoát popup khi bấm ngoài.

**2. Nguồn + Thư viện** (UI-3b/3c/3f): card Nguồn = **1 slot** → `SourcePickerPopup.vue` gộp 2 nguồn **2 tab** · `LibraryCard.vue` icon thư viện cuối dock · header `/studio/library` redesign.

**3. Toolbar canvas** (UI-3g→3j/3p/3r): RegionTools + ContextToolbar **đơn sắc theme** (cream/ink, bỏ brand), icon/cỡ nhất quán, nhóm chuẩn UX, dải phân cách ngang · thêm **tool Lựa chọn (Select)** đầu dock (icon cursor) — **quét/marquee chỉ khi bật Select** (hết đua pan) · **snap xuống status bar** (mặc định 8px, 8/16/24/32) · `exitCanvasTools()` thoát tool khi chuyển tác vụ/thoát ảnh tiêu điểm.

**4. Viewer GalleryModal** (UI-3d/3e/3s/3t): chuyển ảnh **mượt không chớp** (probe + cache `loadedUrls` + crossfade) · info thu gọn · nút **Sử dụng → Prompt Tạo Ảnh** · dải thumbnail cuộn/chọn được (fix pointer-capture) · lưu/khôi phục layer+nhóm+lựa chọn sau refresh · cài đặt status bar tự lưu.

**5. Đa chọn & GROUP = 1 đối tượng** (UI-3n→3aj): shift+click toggle nguyên nhóm · **quét chọn** · `MultiSelectBar.vue` (căn lề 6 hướng · chia đều X/Y · nhóm/tách · tải hàng loạt · xóa) icon Lucide · `selectionUnitCount` (đếm đối tượng) · khung **bbox tím** quanh nhóm · ring layer **xanh sky inner** (outline offset âm) · hệ thống `layerGroups` + `groupId` với rotation/scale cấp group (reset rotation hoàn nguyên) + z-order cả nhóm + thu gọn folder + edit-in-group (Alt+click) · kéo-thả ảnh Outputs vào canvas.

**6. Fix gốc lỗi dai dẳng**: **chọn layer panel** — root cause truyền nhầm **object** thay vì **id** vào `selectLayerWithGroup` → normalize + truyền `l.id` (UI-3aj) · `isSelected` getter→action (fix TypeError màn hình trắng) · bbox lệch (viewport vs local) · `ring-inset` bị che sau ảnh → `outline`.

**Verify:** vite build ✓ mỗi phiên · grep/diff 100% · deploy SSH + verify live asset HTTP 200.


### PHẦN I.2 — Bézier path tool + Card Sửa ảnh (2026-09-07→08 · 13 phiên O–AA · commit `5f8d7bf` → `2ec903b`)

> Nối tiếp PHẦN I (UI-1→UI-3aj). Toàn bộ chi tiết từng phiên nằm trong `STUDIO_REVIEW_PROGRESS.md` (mục "## Phiên O…AA"). Đây là bản gộp theo tính năng.

**A. Công cụ vùng chọn đường cong (Bézier, Krita Pen):**
- **2 tay điều khiển/node** (out `#38bdf8` + in `#f0abfc`, ẩn tay suy biến): kéo node/tay để chỉnh, **Alt = phá đối xứng**, right-click xóa node, Ctrl+Z bỏ điểm.
- **Hiện ngay lần nhấp đầu + preview khi kéo**: `pathDown` push node vào `inpaintPathPoints` (reactive) ngay pointer-down → node hiện tức thì; `pathMove` chỉnh tay in-place → đường cong/tay preview sống.
- **Mở đường bao khi vẽ** (không đóng vòng sẵn): `pathSmooth(pts, closed=false)`; **snap-đóng ở điểm đầu** (node 0 phóng to + guide nét đứt xanh khi hover gần) → **tap = đóng kín, kéo = di chuyển node đầu**.
- **Ctrl+click node → đổi kiểu** xoay vòng **smooth/cusp/sharp** (màu node theo kiểu: tím/cam/hồng); chuyển KHỎI sharp → tự sinh tay điều khiển trỏ về node kế/trước.
- **Chỉnh sửa vùng ĐÃ ĐÓNG**: bấm node vùng (chấm mờ) hoặc nút "Sửa" (pencil + độ trễ ẩn 450ms khi hover đường bao) → nạp lại node để sửa; đóng → cập nhật đúng vùng + **re-bake toàn bộ mask** (`_rebakePathRegions`, mỗi region giữ `_mode` add/subtract).
- **Icon +/− vùng chọn** chuẩn ngành (`selectAdd`/`selectSubtract`) cho freehand/path/magic; ContextToolbar **tô sáng nút chọn** (ring brand) + nút "Hoàn thành" (xanh, chỉnh sửa vùng) phân biệt nút "Xong" thoát.
- **Tỷ lệ & tô màu chuẩn**: mask canvas theo **frameLayout (baseW/baseH)** — 1 px mask = 1 px khung vẽ; **cứng hoá mép** (ngưỡng sel≥128) để Xóa/Tô/Nhân đôi/Nâng che trọn vùng (bỏ viền mờ anti-alias); `willReadFrequently` cho mask ctx (hết warning). Fix layer TRỐNG: `addBlankLayer` cap `baseW/baseH` ≤512 khớp CSS `max-w/max-h`.

**B. Card Sửa ảnh (clean + 1 công cụ):**
- XÓA **Render đa góc**, **Model chỉnh sửa**, **Chọn vùng / Vẽ tự do / Vẽ mask** (cũ).
- Nút **"Vẽ mask"** duy nhất → gọi đúng 1 công cụ **vùng chọn bằng đường cong**; **vẽ xong & đóng → tự lấy vùng chọn làm mask** (`pathClose` finalize khi source=inpaint). Nút "Chỉnh lại" (card + toolbar) mở lại path.
- **Nhận MỌI ảnh từ canvas** (upload/sản phẩm/kết quả/đã sửa): backend tách `handleInpaint(Request, ?Generation)` + endpoint **`POST /studio/inpaint`** (source-agnostic, chỉ cần `source_url`); store `inpaint()` nguồn = `upscaleSrc`, bỏ yêu cầu `previewId`.
- Nút "Vẽ mask" **đẹp mắt** full-width: gradient emerald→teal khi active, viền/nền xanh nhạt khi nghỉ, icon badge + label + sub-label + trạng thái động.

**Verify:** php -l ✓ (controller/routes) · vite build ✓ mỗi phiên · deploy SSH + verify live asset HTTP 200 (commit `2ec903b` mới nhất).


### PHẦN I.3 — Workspace VSCode-style + PWA + Inpaint/Kịch bản quay (2026-09-08 · 22 phiên AB–AV · commit `8e2b9a1` → `7b9a9a4`)

> Nối tiếp PHẦN I.2. Chi tiết từng phiên trong `STUDIO_REVIEW_PROGRESS.md` (mục "## Phiên AB…AV").

**A. Workspace VSCode-style (left card / right dock):**
- **Activity bar kiểu VS Code**: thanh icon dọc cố định (w-14) thay step-nav 1/2/3; mỗi icon mở 1 nhóm card (Tạo ảnh · Fitting Room · Sửa ảnh · Upscale · Kịch bản quay). Bấm icon ĐANG CHỌN → toggle card. Icon AI: logo Studio=bot, Tạo ảnh=sparkles (hoán đổi). Thứ tự: Tạo ảnh trên cùng.
- **Left card + Right dock ẩn được** (`leftPanelOpen`/`outputDockOpen`, lưu `trillfa.bar`); nút chevron rõ ràng. Right dock **viết lại kiểu left** (panel-head + collapsible): icon Nguồn ảnh (mở popup), Thư viện (điều hướng /studio/library), Outputs (toggle dock); panel chỉ hiện Outputs.
- **Kích thước tối ưu không gian**: sidebar `w-80→w-72`, padding thắt; giãn gap nút (activity gap-2, toolbar gap-1.5); `_grabR()` bán kính grab 22px touch/12px chuột (vô hình, dễ bấm).
- **Quản lý title**: `document.title` động theo activity + dự án (`"Sửa ảnh · Dự án · Trillfa Studio"`).

**B. PWA cho /studio:**
- Manifest riêng `manifest-studio.json` (`application/json`, scope /studio) + **icon AI sparkle riêng** (192/512/maskable/apple/favicon).
- Service worker `sw-studio.js` (scope /studio): navigation network-first, asset SWR; KHÔNG cache HTML no-store (fix SW không activate → không installable).
- Đăng ký SW đúng file **`studio/vue.blade.php`** (trang /studio thật, không phải layouts/studio cũ). Prompt cài đặt (`beforeinstallprompt` + banner + nút top bar). Storefront cleanup bỏ qua scope /studio.

**C. Cards + presets:**
- **Kịch bản quay (DirectorCard)**: đổi từ "Video/Ghế Đạo Diễn"; đọc preset `video_scene` từ Prompt Templates (endpoint `/studio/defaults` trả `video_scenes`); GUI thiết kế lại chuẩn card (seg duration/resolution, lưới preset, icon SVG).
- **Card Sửa ảnh (Inpaint)**: chip chỉnh nhanh — **Đổi màu** popup chọn màu · **Thay nền** popup nhập prompt · preset `inpaint` từ Prompt Templates (seed 5: Xóa tạp chất/Làm sạch/Chỉnh dáng/Chất liệu/Xóa vật thể); checkbox giữ mặt/nền; mask lưới mini preview; **Ctrl+Enter gửi**; **undo/redo mask** (snapshot stack 30).
- **Settings/presets**: `inpaint` category thêm vào trang `/studio/presets` (Prompt Templates) để quản lý quick preset Inpaint; trang Cài đặt `/studio/settings` được khôi phục (sau khi thử gộp redirect → hoàn tác).

**Verify:** php -l ✓ · vite build ✓ mỗi phiên · db:seed PresetSeeder ✓ (5 inpaint presets) · deploy SSH + `optimize:clear` ✓ · asset live HTTP 200 (commit `7b9a9a4` mới nhất).

## 0. Tổng kết

- Ghi nhận gốc: **30 area · 214 findings** (gồm 6 bug phát hiện & vá ngay trong các đợt vá: K.4 ×2 · K.8 ×2 · L.2 ×2).
- Kết quả: **đã vá ~60** (§2) · **loại — dương tính giả 12** (§4 + S7/S10 ở §3) · **vô hiệu 24** (findings trên file mồ côi `index.blade.php` đã xóa — H.2–H.4) · 2 file Vue mồ côi **đã xóa** (T5).
- **CÒN MỞ:** ~18 medium · ~45 low/info (§5) · **2 nhóm nhiệm vụ (§1: T1 deploy — việc người dùng · T7 commit báo cáo)**. T2–T6 **đã đóng** ở phiên N.
- Baseline kiểm thử mới nhất (phiên N, đo sau mọi đợt vá): full suite **210 pass / 0 FAIL — xanh toàn bộ lần đầu** · Project 46 · StudioSecurityFixes 11/11 · ShopFlow 64/64 · ImageFallback 8/8 · vite build ✓ · **0 regression**.
- **Phiên N (2026-09-07, đợt sau M)** — **T2**: tái xác minh 11 nhóm §3 bằng workflow 11 agent luân phiên model (qwen3.8-max/glm-5.2/deepseek-v4-pro, retry khi fail) + điều phối xác minh 100% verdict bằng cách đọc lại đúng dòng → **6 nhóm CÒN → vá hết** (S1 critical traversal → helper `studio_safe_public_file()` dùng chung 5 call-site · S3 SSRF `storeRemoteImage` → Http client scheme/timeout/redirect/cap 50MiB + allowlist host tùy chọn · S4 XSS stub → `e()` · S5 `AdminProductController::resolveImagePath` thiếu containment (agent báo SAI — bắt lỗi khi đối chứng) · S8 cleanup scope → 422 · S9 key input → `type=password`) + **phát hiện & vá mới**: cap pixel decompression-bomb trong `studio_image_decode` (`studio.image_max_pixels`, 30MP mặc định) · residual S6 `studio_vision_image_data_uri` · **2 nhóm SAI** (S7 route admin · S10 input admin-only) · **2 nhóm đã vá trước** (S6 chính J.2 · S11 J.3). **T3**: 9 test baseline → 64/64 (root cause: SPA migration — 2 code fix Storefront + 12 blade JSON flags + **khôi phục LD+JSON Product server-side bị mất khi chuyển SPA** — subagent gỡ assertion giấu regression, điều phối bắt lại + vá đúng bằng `seo()->product()`; + thêm HEX flags chống script-breakout mà subagent làm mất). **T4**: CanvasMaskTools pointercancel + Ctrl+Z guard (subagent, SFC_OK). **T5**: xóa 2 component mồ côi. **T6 đóng nốt**: pattern/tryon blade gửi `project_id` (select dự án tải qua `/studio/projects`) · move 1-chạm giữa dự án (nút Chuyển trong GalleryModal) · popover quick-apply 8→20 cuộn được. Test mới `StudioSecurityFixesTest` 11/11 · ImageFallback cập nhật fake https (file:// bị chặn đúng sau vá S3).
- **Phiên M (2026-09-07, đợt sau L)** — Đồng bộ cấu trúc Quản lý dự án xuống **Outputs + /studio/library** + cơ chế **áp dụng 1 dự án cụ thể** + icon đồng bộ canvas: backend +4 điểm additive (tên dự án trong `latest()` + generations của project detail · `project_linked_count` · filter `project_id=none`) · store tách **`appliedProject`** khỏi `activeProject` (đóng workspace không mất áp dụng) + `applyProject`/`unapplyProject`/`openViewer(list)` + attach/detach đồng bộ mọi state · 6 file Vue (LibraryApp · GalleryModal · ProjectWorkspace · OutputModule · StudioApp · StudioIcon +20 icon Lucide) · **đóng nửa đầu T6** (GalleryModal có nút gắn/gỡ dự án) · vá 1 bug tích hợp mới (z-index viewer) · **5 test mới + vá test refgen lỗi từ `be3cfe7`** (T3 còn 8 fail baseline) · full suite **190 pass / 9 fail = baseline** · vite ✓ 781ms · **0 regression**.

## 1. NHIỆM VỤ CÒN MỞ — danh sách duy nhất (gộp từ J.8 + K.7 + K.8 + L.1)

| # | Nhiệm vụ | Loại | Nguồn | Trạng thái lúc gộp (đã kiểm chứng) |
|---|---|---|---|---|
| **T1** | **DEPLOY production**: `234d405` (fix đệ quy) + `be3cfe7` (refgen 422) + `8817f85` (build assets kèm trong `public_html/build/`) | Vận hành — **ưu tiên số 1** | L.1 | MỞ. Bằng chứng production đang chạy code TRƯỚC `234d405`: stack exception **#47290 frame** (chữ ký đệ quy vô hạn `studio_image_decode()`; pipeline thường ≤60 frame) → mọi request ảnh qua `downscaleSource()` trên host vẫn 500. Không xác minh được trạng thái deploy từ máy local. |
| **T2** | ~~Tái xác minh 11 nhóm high/critical~~ | Review | J.8#1 = K.7#3 | **ĐÓNG — phiên N.** Verdict từng nhóm ở §3: 6 CÒN → vá hết (+2 phát hiện mới kèm) · 2 SAI · 2 đã vá trước · 1 giảm mức. Điều phối đọc lại đúng dòng 100% verdict high/critical — bắt được 1 verdict agent sai (S5) + 1 claim giảm mức (S2). |
| **T3** | ~~9 lỗi ShopFlowTest baseline~~ | Test | J.8#2 = K.7#1 | **ĐÓNG — phiên N, 64/64 pass.** Root cause: storefront chuyển Vue SPA — JSON boot bị escape unicode/slash (`@json` mặc định) + 2 bug code thật (contact page hardcode · `staticPage` route `/trang/{slug}`) + **LD+JSON Product server-side bị mất khi chuyển SPA** (khôi phục bằng `seo()->product()` trong `StorefrontController::product`). Blade `@json` giờ `JSON_HEX_*` + `UNESCAPED_UNICODE|SLASHES` (không mở lỗ script-breakout). |
| **T4** | ~~Vá 2 medium sống I.4~~ | Vá | J.8#3 = K.7#2 | **ĐÓNG — phiên N** (subagent + điều phối đọc diff): M6 `onFhEnd` xử lý `pointercancel` như `pointerup` + `onBeforeUnmount` gỡ đủ 5 window listener · M8 guard `e.target` INPUT/TEXTAREA/SELECT/isContentEditable theo đúng pattern GalleryModal · SFC_OK. |
| **T5** | ~~Xóa 2 component Vue mồ côi~~ | Dọn dẹp | I.1 | **ĐÓNG — phiên N**: `git rm` sau khi grep 0 tham chiếu lần cuối ngay trước xóa · vite build ✓. |
| **T6** | ~~Feature mini K.8~~ | Feature | K.8 → M → N | **ĐÓNG HOÀN TOÀN — phiên N**: pattern/tryon blade có select dự án (fetch `/studio/projects`, ẩn khi 401/403) gửi `project_id` — chuỗi đầy đủ đã kiểm chứng: blade → validate ownership (`:4468/:4485`) → `queueGeneration` lưu (`:1502`) · **move 1-chạm** giữa 2 dự án (nút 'Chuyển' trong GalleryModal khi ảnh đã gắn — panel chọn dùng chung) · popover quick-apply 8→20 mục, cuộn được. |
| **T7** | Commit 2 file báo cáo (`STUDIO_REVIEW.md` · `STUDIO_REVIEW_PROGRESS.md`) | Repo | K.7#4 + M | **ĐÓNG — phiên AB**: đã commit `STUDIO_REVIEW.md` (PHẦN I.2) + `STUDIO_REVIEW_PROGRESS.md` (Phiên O–AB) cùng đợt này. |

**Nhiệm vụ cũ ĐÃ ĐÓNG nhờ công việc mới (không theo dõi nữa):**

- J.8#4 — "full-suite `ProcessSignaledException` = OOM/limit hạ tầng": **ĐÓNG + ĐÍNH CHÍNH**. Root cause thật là `[critical]` đệ quy vô hạn `studio_image_decode()` (K.4 — thay thế cơ học 41 site đã thay luôn lời gọi bên trong chính helper mới), không phải hạ tầng; fix `234d405` → 22 test từng SIGKILL sống lại, full suite chạy trọn từ K.5.
- J.8#5 — "working tree nhiều thay đổi chưa commit": **ĐÓNG**. Các phiên đã cuốn vào `af5a86f`/`f35fd00`/`f90c5e9`/`8817f85` (chỉ còn T7).
- J.1 — "tái xác minh + tồn đọng QUEUE A/B": QUEUE A (7 task) + QUEUE B (5 task) **hoàn tất 100%** (H + I) → module phủ ~100% diện tích; 4 pattern đòn bẩy (G.3: decode 41 · res.ok 39 · catch rỗng 26 · getMessage 12) **đã vá hết** (J.2–J.6).

## 2. ĐÃ VÁ — bảng tra cứu (không cần đọc lại phần cũ cho các mục này)

| Finding cũ | Fix | Bằng chứng |
|---|---|---|
| Top-5 #1 · `[high]` path traversal `buildMaskImage()`/`faceDescription()`/`poseDescription()` — đọc file tùy ý qua `source_url` chỉ validate `string` | `safeLocalFile()` — `StudioController.php:3377`: containment `realpath()` + regex charset + chặn `..`; 21 site đọc file theo input đều đi qua | Phiên song song (trước I) · test traversal 17 assertions · xác nhận tồn tại :3377 lúc gộp |
| Top-5 #2 · `[high]` API key material xuống trình duyệt + đường ghi plaintext | `StudioApiKey`: `$hidden=['value']` (:17) + mutator mã hóa `setValueAttribute()` (:26) — invariant tầng model thay vì nhớ tay ở controller | Phiên song song · test 5 assertions · H.6 xác nhận blade keys chỉ render metadata, không echo `value` |
| Top-5 #3 · `[medium]` decode ảnh không giới hạn kích thước — **41 site** (đính chính G.2: báo cáo cũ ghi ~10 là thiếu 4 lần) | Helper toàn cục `studio_image_decode()` (`helpers.php`, cap mặc định **64 MiB**, nhận path hoặc raw bytes) thay cả 41 site | J.3 · `grep '@imagecreatefromstring' app/` = 0 site thực |
| K.4 · `[critical]` đệ quy vô hạn trong chính helper đó (root cause mọi SIGKILL + 500 production) | 1 dòng: trả lại `@imagecreatefromstring` bên trong helper | `234d405` · 5 file test (ImageFallback/Upscale/Look/Region/RegionPaste) pass lại |
| Top-5 #4 · `[medium]` `studioImage()`/`studioImageThumb()` (public, không auth) phục vụ file bất kỳ trong docroot (`.htaccess`, bundle JS) | `studioServePath()` — `StudioController.php:3312`: containment 3 root + **chặn dotfile segment** + **whitelist đuôi ảnh/video**. Giữ root `public_html` vì Hostinger web root khác | J.2 · ngoài phạm vi → 404 json |
| Top-5 #5 · `[medium]` trả nguyên văn `$e->getMessage()` cho client — 6 site response 500 | `studio_fail()`: `Log::warning` server-side + message chung; chi tiết chỉ lộ qua key `debug` khi APP_DEBUG. **Cố ý giữ 3 site 422** (ProjectController:175 · CartApi:37 · CouponApi:28 — message 422 hữu ích cho UI) | J.4 · grep getMessage trong 500 = 0 |
| P5/P6 · `catch` rỗng — 26 site frontend | 22 site → `console.error('studio ... failed', e)` (SettingsApp 2 · ConceptCard 5 · SwapCard 6 · GalleryModal 2 · SourceCard 1 · SourcePanel 1 · StylistCard 1 · store.js 8). **4 site giữ cố ý** (pointer-capture pattern chuẩn: `store.js:1532/1632` · `GalleryModal.vue:91/107`) | J.5 · grep catch rỗng còn đúng 4 |
| P7 · `res.json()` không check `res.ok` — 18 site | `if (!r.ok) throw new Error('HTTP '+r.status)` 17 site (SettingsApp 2 · RefImageCard 2 · ConceptCard 3 · SwapCard 4 · SourceCard/StylistCard/SourcePanel/SourceLibraryPicker 1 mỗi file · store.js loadPalette 1) + bọc riêng SwapCard reload-assets kèm toast | J.6 · OK-path không đổi · vite ✓ |
| Project Management · **11/11 findings** (A gốc :148-173) | F1 reviewer gate thích ứng (owner không tự duyệt KHI hệ có ≥2 Super Admin; Super Admin duy nhất được self-approve + cờ audit `self_approved`) · F2 N+1 thumbnail → relation `latestGeneration()` eager · F3 N+1 count → `withCount`+`loadCount` · F4 rút `user_id`/`status` khỏi `$fillable` (service `forceFill`) · F5 `Rule::in(STATUSES)` · F6 gộp 1 save duy nhất · F7 bỏ hardcode status, dùng default model · F8 **XÓA** `StudioController::storeProject` trùng lặp, route `POST /studio/projects` → `ProjectController@store` · F9 `catch Throwable` · F10 `DB::transaction` bọc destroy · F11 `relationLoaded` chống query trùng | K.1 · subagent độc lập xác minh 10/11 FIXED + 1 PARTIAL (→ K.4) · 53 test Project pass |
| K.4 · `[high]` `latestOfMany()` rơi constraint chained `whereNotNull` (thumbnail eager/lazy bất nhất) | `ofMany(['id'=>'MAX'], fn($q) => $q->whereNotNull('media_url'))` — constraint vào TRONG subquery + test regression assert **giá trị** trên cả 2 path | `f90c5e9` (code) + `8817f85` (test) |
| Luồng duyệt chéo + UX/UI 9 hạng mục | `show`/`transition` mở cho Super Admin dự án bất kỳ (update/destroy vẫn owner-only) · `GET /studio/projects?scope=pending` (403 non-super) + `owner_name`/`can_review` additive · fix leak `confirmTimer` (= finding cũ I.3#5) · `movingId` chống double-click · loading states thật · ESC phân tầng + `role=dialog`/`aria-modal` · ô ghi chú `transNote` → `status_history` · hàng đợi "⏳ Chờ duyệt" · countdown deadline · refetch sau transition ở scope pending | K.2 + K.3 · vite ✓ 9.46s |
| K.8 · `[medium]` IDOR-lite — 4 endpoint nhận `project_id` tùy ý | `Rule::exists('projects','id')->where('user_id', $request->user()->id)` tại generate · renderVideo · pattern · tryon | K.8 · test `test_generation_endpoints_reject_foreign_project_id` → 422 |
| K.8 · `[medium]` UX "dự án hiện tại" vô hình + ảnh không auto-attach | Chip `[pin] <tên>` header StudioApp (click mở workspace, X ngắt) · `generateImage` gửi `project_id: appliedProjectId()` · badge "Chế độ duyệt"/"Đang áp dụng" trong workspace | K.8 · vite ✓ |
| L.2 · `[low]` refgen tạo generation mồ côi khi ảnh nguồn không đọc được (`blob:` URL, file đã xóa — bằng chứng production gen #3 fail 0.04s) | Pre-validate `resolveLocalImage()` ngay trong request → **422** kèm hướng dẫn chọn lại ảnh, KHÔNG tạo generation; bonus hoist `downscaleSource()` khỏi loop variants (trước ghi N file `ds-*` trùng nội dung) | `be3cfe7` · test `test_refgen_rejects_unresolvable_image` · 8/8 pass (27 assertions) · repro tinker: blob: → 422 + 0 generation |
| L.2 · `[low]` `input[type=color]` bind `''` → Chrome warning CSS mỗi lần mở form dự án | `:value="form.color || '#4a7a90'"` + `@input` — fallback chỉ tầng hiển thị, model vẫn giữ `''` | `8817f85` · vite ✓ |
| H.1 · `[low]` `index.blade.php` FILE MỒ CÔI — 1.626 dòng/140 KB dead code (0 đường render; UI Alpine đã thay bằng Vue) | **XÓA file** → 24 finding H.2–H.4 vô hiệu (vốn không có tác động runtime) | `85b9f91` · xác nhận file không còn trong tree lúc gộp |
| **M** · outputs + `/studio/library` chưa "nói ngôn ngữ dự án" + cơ chế áp dụng ngầm (mở detail ≡ áp dụng; đóng detail = mất áp dụng) + icon emoji lệch canvas + `attachGenerationToProject` mồ côi UI (T6) | **Backend (additive):** `latest()` + project detail generations trả kèm `project` (tên) · `counts()` thêm `project_linked_count` · library filter `project_id=none` (ảnh chưa gắn). **Store:** tách `appliedProject` (tồn tại độc lập workspace) khỏi `activeProject` (đang xem) · `applyProject` (chặn dự án người khác) / `unapplyProject` · `clearActiveProject` chỉ đóng detail · `openViewer(g,list)` + `viewerList` · `attachGenerationToProject` đồng bộ libraryItems/generations/viewer/viewerList/activeProjectGenerations/count. **UI (6 file Vue):** LibraryApp = filter dự án (dropdown + 'none') + badge dự án click-lọc + chip dự án hiện tại + nút áp dụng khi đang lọc + card thống kê 'Đã gắn dự án' · GalleryModal = field Dự án + khối gắn/gỡ (nút nhanh gắn vào dự án hiện tại) · ProjectWorkspace = nút 'Áp dụng dự án này' tường minh + quick-apply pin trên board/list + outputs click mở viewer + hover gỡ khỏi dự án (chặn reviewOnly) · OutputModule = toggle lọc theo dự án áp dụng (`visibleGenerations`) + chấm màu dự án trên item · StudioApp = chip đọc `appliedProject` + quick-apply popover ở header + mobile bar icon · StudioIcon +20 icon Lucide-style (pin/pinOff/eye/link/unlink/filter/calendar/tag/archive/kanban/grid/download/copy/folderOpen/clock/users/list/briefcase/play/alertTriangle) thay toàn bộ emoji khu vực dự án. **Bug tích hợp vá trong đợt:** GalleryModal z-70 < workspace z-95 → viewer mở từ outputs dự án bị che (→ z-110) · pin board chỉ hiện khi hover (→ đang áp dụng luôn hiện) · fallback màu dự án null. **Giới hạn đã ghi nhận:** attach chỉ hiện khi ảnh chưa gắn dự án → "chuyển dự án" = gỡ rồi gắn lại (không có move 1-chạm); popover quick-apply 8 mục đầu | Phiên M · test `StudioLibraryProjectTest` **5/5 pass** (16 assertions) + Project suite 54/54 + full suite **190 pass / 9 fail baseline** · 6/6 file SFC_OK · vite ✓ 781ms |
| **N** · T2 6 nhóm CÒN (S1 critical traversal · S3 SSRF · S4 XSS stub · S5 admin traversal · S8 cleanup scope giả · S9 key trần) + phát hiện mới: pixel-cap decompression bomb · S6 residual vision data-uri + T3 storefront SPA (9 test baseline) + T4 CanvasMaskTools 2 medium + T5 2 component mồ côi + T6 nốt (pattern/tryon gửi project_id · move 1-chạm · popover 20) | **Bảo mật:** helper `studio_safe_public_file()` dùng chung (5 call-site: 3 resolver ImageAIService · safeLocalFile ủy quyền · resolveImagePath admin · vision data-uri) · `storeRemoteImage` qua Laravel Http (scheme http/https · timeout 30s/connect 10s · redirect ≤2 · cap 50MiB · allowlist host tùy chọn) · `e()` toàn bộ biến user/AI trong stub HTML · cleanup scope validate 422 · key input `type=password` · cap pixel `studio.image_max_pixels` (30MP) trong `studio_image_decode`. **T3:** StorefrontBridge contact đọc settings · StorefrontController `staticPage` xử lý slug `/trang/{slug}` · **khôi phục LD+JSON Product server-side** (`seo()->product()` — mất từ khi chuyển SPA, subagent từng gỡ assertion giấu regression) · 12 blade `@json` thêm UNESCAPED_UNICODE|SLASHES **kèm giữ HEX flags chống script-breakout** · test: wishlist public · qwen vision expectation đúng code. **T4/T5/T6:** CanvasMaskTools pointercancel + guard e.target · xóa SourceCard/PaletteTextureCard · pattern/tryon select dự án gửi project_id · GalleryModal nút Chuyển (move 1-chạm) · popover 8→20 | Phiên N · test mới `StudioSecurityFixesTest` **11/11** · ShopFlow **64/64** · ImageFallback 8/8 (fake https thay file://) · full suite **210 pass / 0 FAIL** · vite ✓ |

## 3. High/critical — KẾT QUẢ TÁI XÁC MINH T2 (phiên N, workflow 11 agent luân phiên model + điều phối đối chứng 100%)

| # | Nhóm | Verdict | Xử lý phiên N |
|---|---|---|---|
| S1 | `[critical]` ImageAIService traversal — 4 resolver | **CÒN → ĐÃ VÁ** | Helper dùng chung `studio_safe_public_file()` (helpers.php): chặn `..` + charset whitelist + realpath trong public/ hoặc storage/app/public; 3 resolver + StudioController::safeLocalFile (ủy quyền) + S5/S6-residual đều qua đây — `/storage/../../.env` → null, không còn copy .env ra file public. Test 11/11. |
| S2 | `[high]` unbounded memory per-pixel loops | **GIẢM MỨC + vá lỗ hổng liên quan** | Claim gốc không đúng kỹ thuật: imagecreatetruecolor → imagecolorallocate không bồi palette. NHƯNG đối chứng phát hiện: không cap SỐ PIXEL — PNG 64MiB giải nén thành ảnh trăm MP → OOM ngay tại decode → **vá bằng cap pixel `studio.image_max_pixels` (mặc định 30MP, 0=tắt) trong `studio_image_decode`** (phủ 43 call-site). |
| S3 | `[high]` storeRemoteImage @file_get_contents | **CÒN → ĐÃ VÁ** | Laravel Http: chỉ http/https · timeout 30s/connect 10s · redirect ≤2 giới hạn protocol · cap 50 MiB · allowlist host tùy chọn setting `studio_remote_image_hosts`. 3 test ImageFallback cập nhật: fake file:// → https CDN giả (file:// bị chặn ĐÚNG). |
| S4 | `[high]` stored XSS stub ProductAIService | **CÒN → ĐÃ VÁ** | e() mọi biến user/AI trong stub()/stubDescription()/stubRefine() (HTML render v-html storefront); field plain-text không escape. Test 2 case. |
| S5 | `[high]` ProductAI đọc file cục bộ | **CÒN → ĐÃ VÁ** (agent verdict SAI — điều phối bắt lỗi khi đối chứng: `resolveImagePath` ở **AdminProductController:283** chứ không phải service, KHÔNG containment) | `resolveImagePath` → `studio_safe_public_file()` — admin thường không còn exfil .env lên AI provider qua image_url traversal. Test riêng. |
| S6 | `[high]` helpers studio_image_url / studio_vision_image_data_uri | **ĐÃ VÁ TRƯỚC (J.2) + residual ĐÃ VÁ** | Route serve → studioServePath containment (kiểm chứng :1719→:3312). Residual: studio_vision_image_data_uri còn loop is_file thô → đổi qua helper dùng chung + test. |
| S7 | `[high]` orphan scan unscoped | **SAI (dương tính giả)** | routes/web.php:94 — cả scan + cleanup trong group auth+admin+nostore; cursor toàn cục là CHỦ ĐÍCH (admin quét toàn hệ thống); cleanupOrphans chỉ xóa file không ai tham chiếu trên TOÀN dataset → không xóa nhầm file user khác. |
| S8 | `[high]` cleanup scope không whitelist | **CÒN → ĐÃ VÁ** | Controller validate `in:orphans,junk,old` → 422 thay vì {deleted:0} + ok:true giả. Test riêng. |
| S9 | `[high]` SettingsApp UI key exposure | **CÒN (một phần) → ĐÃ VÁ** | Input key → type=password + autocomplete=new-password. Phần guard admin client-side: route đã admin-only server-side → không thêm (ghi nhận). |
| S10 | `[high]` VirtualTryOn prompt injection | **SAI (dương tính giả)** | background/modelDesc từ store_outfit settings (admin) · tone whitelist qua toneInstruction match · route admin duy nhất + abort_unless owner 403 — không có đường user input tới prompt. |
| S11 | `[high]` StyleSuggest downscaleBase64 traversal | **ĐÃ VÁ TRƯỚC (J.3)** | studio_image_decode (is_file + cap 64MiB + imagecreatefromstring — .env fail decode → null) + caller faceDescription qua resolveLocalImage/safeLocalFile containment. |

**Kết luận T2:** 11/11 nhóm có kết luận — 6 CÒN vá hết · 2 SAI · 2 đã vá trước · 1 giảm mức (kèm vá lỗ hổng pixel-cap phát hiện trong lúc đối chứng). Không còn nhóm high/critical nào mở trong module Studio.

| Claim bị loại | Bằng chứng bác bỏ | Nguồn |
|---|---|---|
| `[high]` "`config/studio.php` đọc `env()` lúc parse → sau `config:cache` rotation key bị bỏ qua" (top-5 cũ #3) | Pattern chuẩn Laravel (224 vị trí `env()` trong config/ — nếu là lỗi thì cả framework là lỗi); đường rotation THẬT của module là **DB-first**: `studio_api_key()` ưu tiên registry DB mã hóa `StudioApiKey` → setting DB `api_<service>_key` → config fallback; đổi key từ Settings có hiệu lực ngay bất kể config:cache. Lỗi thật của họ này (`env()` NGOÀI config/) chỉ 3 vị trí, đều vô hại | G.1 + H.0 |
| `[high]` "StylistData `page()` public cho mọi khách, không middleware" | Route nằm TRONG group `[auth,admin,nostore]` (`web.php:94,128,192-196`); group public (:202) chỉ 2 endpoint read-only. `glm-5.2` tự đọc routes mô tả đúng; `qwen3.8-max` khẳng định sai | E |
| `[medium]` "csrf-mass-assignment `storeProject()`" | `validate()` chỉ trả đúng 2 key `name`/`base_concept`; `user_id` do relation `projects()` gán | D |
| `[high]`+`[medium]`×2 blade XSS (`presets.blade` "render raw" · `vue.blade @json` · `api.blade $service nội suy thô") | `{{ }}` escape mặc định; `@json` dùng `JSON_HEX_TAG|APOS|AMP|QUOT`; `$service` từ mảng hardcode; grep `{!!` toàn views/studio = **0** | F |
| `[medium]` "StylistCatalog DDL chạy mỗi request, unauthenticated" | `Schema::create` chỉ chạy khi thiếu bảng (:30 — đánh đổi có chủ đích cho shared hosting, docblock :18); route trong group admin | F |
| `[medium]` "ProjectWorkspace load/open/move/remove không có user feedback" | Store bắt + toast (`store.js:1138-1140`, :1146,:1151) | I.8 |
| `[low]` "StudioIcon không guard r.ok" · "LoadingSpinner progress không clamp" | Tự đính chính khi re-read: guard `r.ok ? r.text() : ''` có sẵn · clamp `Math.min/max` có sẵn (:40); residual thật = `.catch(() => '')` nuốt lỗi | I.5, I.6 |
| `[medium]` store.js IDOR generation/project id (A :135) | Server-side enforce đã xác minh: generation owner-scoped `abort_unless` (C :194,988,1295,1377,1397,3681...) + K.8 hardening `project_id`; chính finding kết luận "front-end cannot close this gap" | A→C→K.8 |

## 5. Backlog finding CÒN MỞ — medium/low/info (một dòng/mục)

> Quy ước: `[s]` file:vị trí — triệu chứng → hướng fix · `(↓)` = đã hạ từ medium sau xác minh · nguồn ghi cuối dòng (phần cũ). Chi tiết từng dòng: đọc nguyên bản theo hướng dẫn đầu file.

### 5.1 Medium còn sống (ngoài §3)

- `[medium]` StudioController `swapModel()` :3186-3203,:3298 — tạo Generation `credits_cost=1` rồi nâng `max(1,calls())` (2-3), KHÔNG trừ `credits_balance` (`decrement` duy nhất ở `queueGeneration` :1501) → `credits_left`/`studio_usage()` báo sai. Fix: đi qua queueGeneration hoặc decrement tường minh. (Đã hạ từ high: credit là số liệu theo dõi, không phải cơ chế chặn — tool nội bộ.) — D
- `(✓grep)` `[medium]` StudioController `reconcileStuckCredits()` :1409-1422 — SELECT processing >30 phút → update + `increment('credits_balance')` không transaction/lock/conditional update, gọi đầu mỗi `queueGeneration()` → **double refund** khi 2 request đồng thời. Fix: chỉ hoàn tiền khi conditional update đổi đúng 1 dòng, hoặc `DB::transaction`+`lockForUpdate`. — D
- `[medium]` StylistDataController :88-104,:138-153 — check-then-act `slug`/`key` không atomic + `sort_order=max+1` race → trùng bản ghi. Fix: unique index + bắt constraint → 422 + transaction. **Cùng pattern** `StylistCatalog::savePreset()` :252-259 (dedup trên cột TEXT không index + catch nuốt lỗi). — E + F
- `[medium]` store.js `api()` :262-267 — nay đã throw khi `!res.ok` (✓grep) nhưng VẪN không check `res.redirected`/content-type → login-redirect trả HTML 200 parse thành `{}` và được coi là thành công ở MỌI caller mutation. Fix: guard như `deleteGen` :945. — A
- `[medium]` store.js `loadMoreLibrary` :977-1003 — increment `libraryFilters.page` hai lần (trước gọi + `d.current_page || page+1` :995) → nhảy cóc trang khi backend không trả `current_page`. Fix: page chỉ lấy từ response. — A
- `[medium]` store.js prompt injection: `renderVideo` :610-616 nối prompt user vào template 'Cinematic fashion catwalk' + generateImage/refgen/translate forward raw text; output AI tái nhập prompt → delimit/framing untrusted text server-side. — A
- `[medium]` store.js forward URL ảnh tùy ý (reimagine/refgen/suggestStyle/removeBackground/inpaint `source_url`/runSwap) tới endpoint fetch remote + KHÔNG cap số lượng (variantCount/poses chỉ `Number()||1`, compose chỉ ≥2) → allowlist same-origin/data:/blob: client-side + max tường minh (variants ≤8 · poses ≤12 · images ≤4) + toast. (Local path server đã qua `safeLocalFile`; remote URL = §3#3.) — A
- `[medium]` store.js XSS gián tiếp qua toast :274-280 — chuỗi server/AI (d.name :1157, status_label :1186, styles/garment/background :1972-1974, preset/layer names) → `flashMsg` → Alpine toast. Fix: xác minh toast render `textContent` (không innerHTML) + sanitize. — A
- `[medium]` StylistService `refine()` :67-92 — `$promptEn`+`$answers` nội suy heredoc vào LLM instruction → delimiter + validate JSON output theo schema. — A
- `[medium]` StyleSuggestService N+1: `matchPresets()` :312-323 re-query `Preset::category()` trong nested loop + `suggestViaColor()` :433-437 5 query riêng → 1 lần `Preset::all()->groupBy('category')` hoặc cache catalog. — A
- `[medium]` StyleSuggestService `suggest()` :43-61 — catch loop vứt context exception gốc (status/model đã thử) → aggregate vào RuntimeException/log cuối. — A
- `[medium]` VirtualTryOnService `$hasVision` :328,:429 — không phân biệt "không key" vs "key hỏng" → best-of-N âm thầm degrade về first-candidate; log warning đúng nguyên nhân. — A
- `[medium]` GeminiService `callGemini()` :129-138 — non-2xx (429 quota/401/403) silent, không backoff/key-rotation như ProductAIService → log status+body + detect 429. — A
- `[medium]` ImageAIService blocking `sleep()` trong request path: `callDashscopeAsync` :554-579 (sleep(4) tới 180s) · `swapEdit` :1042 · `swapFace` :1077 → chiếm PHP-FPM worker, exhaust pool dưới concurrent; dời polling/backoff sang queued job. — A
- `[medium]` ImageAIService info leak: provider body thô (240 char) + exception message vào `dashscopeError` :513-575,:960 rồi ra user qua RuntimeException :70,:115-119 → log server-side, message generic. — A
- `[medium]` ProductAIService prompt injection `brandContext()` :60-106 (About page = vector stored, đè mọi generation) + `buildRefinePrompt`/`buildPrompt` nội suy name/hint/tags không delimiter. — A
- `[medium]` StudioLibraryService cụm perf/correctness: `counts()` :75-95 = 7+ query + full disk scan + cursor 4 bảng mỗi `/library/data` (→ grouped COUNT + orphan đếm ở endpoint admin throttled) · `deleteUploadedFiles` :249-272 TOCTOU (`safeUnlink` đã mitigate — tùy chọn `clearstatcache`) · `normalizeUploadRel()` :288 nên reject `..` tường minh (defense-in-depth) · LIKE search :49-51 không escape `%`/`_` (cùng fix với StudioController :3594). — A + C
- `[medium]` config/models (B): migration `studio_assets` không `user_id` (gap authorization nếu per-user) + không index `(type,sort)`/unique `path` · `studio_api_keys_for()` :377-389 load MỌI key rồi filter in-memory mỗi lookup (multi-valued index JSON `scopes` hoặc chấp nhận khi <50 key) · đường key env/config luôn plaintext (prod: DB-only hoặc document) · `studio_api_key()` :430 falsy coalescing (`false`/`0` → null) chưa document.
- `[medium]` RefImageCard.vue :223 — `v-html="a.svg"` trong `<svg>`: an toàn HÔM NAY (anglePresets hardcode :112) nhưng là latent XSS sink ngay khi array đến từ API như faces/poses (:42-50) → render `<path :d="a.d">`. — C
- `[medium]` StudioController `download()` :3687-3695/`palette()` :3724-3726 — `storage_path('app/public/'.$path)` từ `media_url` không guard `..`/charset (owner-scoped nên impact hẹp; poisoned DB row thì traversal) → dùng chung 1 guarded resolver. **Cần tái xác minh**: có thể đã được phủ trong 21 site `safeLocalFile`. — C
- `[medium]` StudioController suppressed GD failures :268,:270,:424,:444 + các site `@` — trả null âm thầm ("no mask"/"no description") không log (J.3 giải quyết phần size; phần silent-null còn) → log warning kèm path trước khi return null. — C
- `[medium]` SwapCard.vue :81 `delAsset` build `'/studio/assets/'+a.id` không validate (backend DELETE là phòng thủ thật) → `encodeURIComponent` + int dương; SuggestCard.vue :21-24 `applyPrompt()` đọc `store.suggestResult?.prompt_vi` bypass computed `r` đã null-guard. — B
- `[medium]` StylistCard.vue :40 — `v-html` render `stylistFeedback` (HTML từ LLM) → **DOM XSS sink THẬT trong lớp Vue** (blade `{!!`=0 nhưng Vue thì có) → bỏ v-html, render text/escaped hoặc DOMPurify allowlist chặt. — I.5
- M6 + M8 (CanvasMaskTools) → đã tách thành **T4**. — I.4

### 5.2 Low

**Backend PHP:**

- ImageAIService: prefix API key `substr($key,0,8)` vào log info/warning 11 site (:732-966) → hash không đảo ngược · `@` suppression lan rộng (:458/601/1085, :330/339, :315, :1109/1406) → check return + log · `fitToSourceSize` :1165 chia `$sw/$sh` không guard 0 · provider output chỉ sniff 3-byte magic trước khi lưu public → `getimagesizefromstring`/finfo.
- ProductAIService: `imageBase64()` :990 `@file_get_contents` false → cast `''` → gửi payload rỗng lên AI, wasted call → check + return null · `brandContext()` :74-93 tới 9 lệnh `setting()` → batch · `record()`/`failureReason()` :176-181,:1186 raw body 120 char về admin UI · perf: decode full-res trước downscale trong `imageBase64`/`offlineAnalysis` (cap 64MiB của J.3 đã chặn case cực đoan; residual pre-scale).
- GeminiService: timeout cố định 90s/60s không wall-clock budget chung → stack nhiều lần = 504 gateway → shared deadline như `ProductAIService::startBudget`.
- VirtualTryOnService: `$lastModel`/`$calls` instance property trên (có thể) singleton → telemetry sai dưới concurrent → scoped binding hoặc reset đầu method.
- StudioLibraryService: `uploadedFiles()` :196-244 glob không limit + `getimagesize()` mọi file → paginate/cache · `StudioAsset::$fillable` chứa `path` — review controller create/update validate path · `deleteGenerationFiles()` :317 chỉ xóa `media_url`, base/mask dồn cho orphan scan (scan hiện unscoped — §3#7).
- StylistService :239-241 — log body 160 char có thể chứa fragment key nếu provider echo → strip `/sk-[a-zA-Z0-9]+/`.
- StudioController: `studioImageThumb()` :1767-1810 cache thumbnail không cap tổng (path enumeration fill disk — mitigation sẵn có: size whitelist + bail >4096px) → cap dir + cleanup pass · LIKE `%`/`_` :3594 → `addcslashes` · SSRF upscale URL :3037/:3079 (cùng gốc §3#3) · 3 map provider-status near-duplicate đã drift (:3844/:3932/:4336) → 1 nguồn `providerStatus()`.
- Models/wiring: `StudioOutfitSetting::$fillable` chứa `user_id` → set từ `auth()->id()`/`creating` event · `StudioBridge` singleton nhưng `app()` mỗi call → resolve ở constructor · `StudioModuleServiceProvider` merge vào `studio_module` tách biệt `studio` không document · `studio_models.priority` signed int (âm phá ordering) → unsigned/validate.
- Console (I.7): `CleanStudioStorage` :30-33,:59 không decode `%XX` khi match referenced (↓ — hiện writer đều UUID-ASCII nên chạy; fragile nếu đổi sang slug → `rawurldecode` hoặc allowlist) · reference set chỉ 3 cột + protectedDirs hardcode (↓ — enumerate gần đủ; residual drift → CI test/document inventory) · `ProcessStudioGenerations` :22-28 `dispatchSync` không try/catch mỗi item (↓ — fallback manual idempotent; job throw → abort loop + log nhầm "Processed") · `CleanStudioStorage` không đăng ký `withCommands` (auto-discover cứu; sửa `withCommands` chặt sẽ orphan) · `--limit=10` default không `--all`/auto-loop.
- CreativeDirectionService (I.2): preset `prompt_injection` bị token model đè âm thầm qua `array_merge` :140-143 (↓ — docblock chủ đích "model free to write rich prompts"; căng thẳng với "Follow every preset tag verbatim" :72 → nếu preset authoritative: merge preset-priority + cap dài + full-signature guard) · `build*Prompt` :191,:211 đọc `tokens['creative_level']` không tồn tại → fallback luôn level 6 (↓ — đường chính dùng level thật :363,:369) · `ensureSignature` :256-261 hai nhánh trả cùng `$tail`, `suppress:true` :158 vô nghĩa → xóa hoặc làm khác biệt thật · `(string)` cast :178-180,:292 khi LLM trả array → warning + chuỗi "Array" lưu payload → `is_string` guard · `tokens()` :31 `strval` nested array → conversion Error hủy cả request → lọc `is_scalar` · không cap độ dài `userPrompt`/`idea`/`style_notes` :361-384 (endpoint có max:4000 nhưng field phụ/suggest thì không) → clamp ~2.000 ký tự + log truncate.

**Frontend JS/Vue:**

- store.js: catch im lặng `load()`/`loadDefaults`/`processQueue`/`renameLayer` (J.5 đã console.error 8 site — verify còn residual "rename fail mà UI báo thành công") · `saveLayerLayout` :1903-1920 ghi CẢ canvasLayers (data-URL multi-MB) vào localStorage mỗi mutation → quota ~5MB nuốt im lặng + jank → chỉ persist URL/metadata hoặc debounce · `librarySelectOld` :1038-1042 phụ thuộc `created_ts` có thể thiếu → fallback `created_at`/id · `translate` :1960 lưu cả object khi thiếu `text` → `String(d.text ?? d ?? '')` · `pollGeneration` :1984-2013 poll 500ms không max-attempt/backoff/deadline → gen kẹt 'processing' poll vô hạn.
- SettingsApp.vue: form ref dùng chung key+model tab → save gửi mọi field bất kể tab · (J.5 đã fix nuốt lỗi load/save 2 site).
- SwapCard.vue: `onMounted` auto-select model đầu mutate `store.swapModelIds` từ component → dời vào store · `setInterval` 1s chạy cả khi không có swap → chỉ start khi `swapStartTs>0`.
- ConceptCard.vue :93-114 — `loadDraft()` restore localStorage draft không validate type/range (prompt không string-check; creativeLevel/texture/variantCount/body* không clamp) → validate+clamp như input. ComposeCard.vue :148-153 `res.json()` không check `res.ok` — **J.6 vá ConceptCard 3 site, ComposeCard không có trong danh sách → kiểm tra lại**.
- GalleryModal.vue: `doDelete` :29-40 không pending flag + nút confirm :375 không `:disabled` → duplicate DELETE (↓ — :34 `if(!ok) return` chứa failure path, :37 clamp index; smoke `kimi-k3` tái xác nhận) · `[low]` MỚI (code phiên M): `clipboard.writeText` :126 không `await` → try/catch không bắt được rejection, toast "Đã sao chép" giả khi permission denied/insecure context → async + await · `[low]` MỚI: `onKey` capture :195-206 `stopImmediatePropagation` mọi phím ngoài input — CHỦ ĐÍCH (comment :194) nhưng guard :197 thiếu `VIDEO` → arrow đoạt quyền seek video trong modal; các window handler khác bị diệt khi modal mở = thiết kế chấp nhận · ~~`watch(items)` reset `idx=0`~~ VÔ HIỆU — phiên M refactor thành `watch(current.id)` :22.
- SourcePanel.vue — ~~`:29` `toDataURL` taint~~ VÔ HIỆU (refactor: file giờ là product picker, saveMask không còn); residual: `loadProducts` :11 fail → chỉ `console.error` + render empty state "Chưa có sản phẩm." :84 gây hiểu lầm, không chỉ báo lỗi/retry (residual của fix J.6 tối thiểu — smoke `kimi-k3` xác nhận) → error state riêng + nút retry.
- UpscaleCard.vue :16 — `addGen` hardcode `status:'completed'` + `credits_cost:0` bỏ qua lỗi upscale thật → lấy từ response.
- OutputModule.vue :8 — anchor `download` với URL remote chỉ hoạt động same-origin; `media_url` provider remote → mở tab thay vì tải → fetch blob server-side.
- RegionTools.vue :12 — `removeRegion` splice theo index (race async đổi list → splice nhầm) → theo `id`.
- StudioIcon.vue — residual `.catch(() => '')` nuốt lỗi fetch (sau khi claim chính bị đính chính) → chỉ log.
- useStudioThumb.js: :11 build `/studio/image-thumb/{path}` từ `media_url` thô không `encodeURIComponent` (server guard có — J.2) · :18 `onThumbError` không `this.onerror=null` → loop spam khi placeholder thiếu (cùng pattern library.blade :67) · :30 `dataset.fallback` thiếu → chuỗi rỗng → img không src → default placeholder.
- LoadingSpinner.vue :27,46 — inline `style animation: 'loadingPulse...'` tham chiếu tên keyframe đã bị scoped-style rewrite (`loadingPulse-data-v-xxx`) → **animation không chạy** (pitfall Vue scoped keyframes) → `:class` hoặc bỏ scoped block keyframe.
- Vite entries (I.7): `app.js` :10,14 empty catch SW-unregister + `mount('#studio-root')` không guard element · `settings.js`/`stylist-data.js` **thiếu** block teardown legacy SW (`app.js` :7-16 + `library.js` :5-8 có) → page còn bị `/sw.js` cũ kiểm soát có thể serve stale HTML → extract `kill-legacy-sw.js` import cả 4 entry.
- ProjectWorkspace.vue (I.3): `toggleArchived` :118-121 call thứ 2 trong single-flight `loadProjects` early-return **bỏ qua refresh** → list stale tới lần load kế (↓ — cơ chế gốc "stale response last" sai) → re-call trong `finally` hoặc request-token · `openProject`/`move` :97-104 không in-flight guard → double-click 2 card, `activeProject` last-write-wins (↓ — move bị chặn server-side) → disable khi pending + discard response id cũ · grouped map :28-33 object thường index bởi `p.status` server → status `__proto__`/`constructor` làm push throw, vỡ board → `Object.create(null)`/`Object.hasOwn` · `submitEdit` :92 resolve target lúc submit thay vì snapshot id lúc `openEdit` → activeProject đổi giữa chừng thì edit hạ sai project.
- ContextToolbar.vue :8,97 — `hasBox` computed chết · nút reopen đọc `_inpaintMaskKind` underscore-private → xóa hoặc expose getter.
- LibraryApp.vue :56 — `confirmTimer` auto-cancel 5s không clear ở unmount → stale callback/memory leak (K.3 chỉ fix họ này bên ProjectWorkspace) → `clearTimeout` trong `onBeforeUnmount`, cân nhắc bỏ auto-cancel cho destructive action.
- settings.blade.php (H.5/H.6): nút Lưu không guard double-submit (:225,:321 — UX thuần) · :461 `onclick="studioTestModel(this, {{ $id }})"` nội suy id vào inline JS, Blade không escape JS context (↓ — id là int PK + chỉ render `@if($id)`, breakout không thực tế) → listener ngoài · :465,:546,:594,:659 DELETE form `onsubmit="return confirm('...«{{ $name }}»...')"` — tên chứa `'` vỡ confirm → **handler throw, submit vẫn chạy, mất bước xác nhận** → `data-confirm`+`@json` · :716-717 `.then` không guard `r.ok` → 500 non-JSON throw xuống catch message chung.
- library.blade.php (H.7): :33,:41 `@change="this.form.submit()"` CHẾT trên Alpine 3.17.1 (`this` không phải element → TypeError nuốt; đã xác minh node_modules) — có nút submit thủ công :52 nên chỉ mất UX → `$event.target.form.submit()` · :18,:23 toàn bộ blob `meta` xuống client không biểu thức nào tiêu thụ (genMeta chỉ dùng 7 field) → phơi bày không cần thiết + phình payload → bỏ/whitelist · :23,:57-62 `del()` xóa item Alpine nhưng empty-state là `@if` server-side → xóa item cuối để lại lưới trống không message · :67,:105 `onerror` không tự gỡ → placeholder thiếu thì loop spam → `this.onerror=null`.

### 5.3 Info (chọn lọc — nhóm chủ đề)

- **a11y**: BaseModal :6-13 + CompareSlider :13-35 + GalleryModal :55,120 — không `role=dialog`/`aria-modal`/focus trap/ESC; CompareSlider keyboard-unreachable (div draggable không `role=slider`).
- **Badge trạng thái thiếu nhánh `cancelled`**: library.blade :119 (modal hiện "Đang tạo" cho gen đã hủy — lưới :115 đúng) + OutputModule :30.
- **ImageAIService**: `sizeFor()` :1485-1497 nhận `$resolution` không dùng (chỉ `$ratio`) → map hoặc bỏ param · dead code `$base = $base = rtrim(...)` :497,:533.
- **CreativeDirectionService** :282 — creativity ≥7 `str_replace` gỡ cụm đuôi để lại dấu `, ` mồ côi trong negative prompt → build từ parts+implode · :366-367,:385 `prompt_prefix`/`prompt_suffix` admin-editable inject mọi prompt không audit log (blast radius nhỏ — tool nội bộ) → log thay đổi 2 config key.
- **GeminiService/ProductAIService**: `generateCreativeDirector()` :25-41 `$creativeLevel` không clamp ở entry (phụ thuộc creativityDirective) · `attempt()` :431-447 unknown provider `match default → null` skip im lặng, mask lỗi config → log/validate `product_ai_providers()`.
- **StylistService `chat()`** :214-268 — `break` ở model-not-found :237 và catch :242 skip mọi key còn lại của model khác → `continue 2`.
- **settings.blade** — XÁC NHẬN ÂM TÍNH quan trọng (giữ nguyên trạng): tab keys/models KHÔNG render giá trị key thô, chỉ metadata + `key_prefix`; đừng bao giờ thêm `@json($api_keys)`. Cả 3 form POST có `@csrf`.
- **Vue shells** `library-vue`/`settings-vue` :3-6 — sạch (không payload inline/secret; `@vite` đúng); tùy chọn `noscript` notice.
- **library.blade :23,134-135** — 1 dòng `x-data` ~1,5k ký tự giữ toàn bộ logic modal/zoom/xóa + endpoint hardcode ('/studio/generations/', '/studio?gen=') → vỡ khi deploy subdirectory; dời sang `resources/js` + `route()` + ESC đóng modal.
- **testApi/testModel (C :3400-3551)** — relay response provider sống về browser → chỉ trả `{ok,status,message}`, strip error body (có thể echo header/key fragment).
- **StudioController monolith** — 4.732 dòng / ~90 endpoint trộn HTTP+GD+provider client → extract `pngBytes`/`buildMaskImage`/`extractPalette`/`downscaleSource` + provider calls sang services.
- **Good practices đáng giữ** (C): file write UUID-named dưới `studio/` · `assetDestroy()` :1722 containment check (nên promote thành shared resolver) · `refImageDelete()` :1669 `basename()` · `uploadRef` validate `image|max:8192` · mọi outbound provider call có timeout tường minh.

## 6. Bài học quy trình (nén — bản đầy đủ: `DELEGATION_PLAYBOOK.md`)

1. **KHÔNG dùng `schema` trong `agent()`** của workflow — model bọc JSON trong fence ```json → engine không trích được `structured` → `agent()` trả `null`. ĐÂY mới là nguyên nhân thật của 8 task "thất bại" lượt 1+2 (không phải rate-limit — child vẫn `turn/end=completed`; đính chính qua log session zstd). Contract text `AREA:/SUMMARY:/FINDING:` + parser bao dung ở host → 0 fail.
2. **Kích thước task**: ≤6 task/lượt (= `maxConcurrentAgents` máy 8 CPU) · ≤60 KB / ≤1.200 dòng mỗi task · **grep liveness trước khi xếp hàng** — đừng review file mồ côi (bài học H.1 `index.blade.php` 140 KB + I.1 2 Vue orphan), cũng đừng bỏ sót file sống.
3. **Xác minh điều phối là BẮT BUỘC bất kể model**: round H **0/9** medium sống sót nguyên bản (6 hạ do file mồ côi, 3 sai cơ chế); round I **2/12** sống sót. Hai model sai đúng chỗ model kia đúng: `qwen3.8-max` rẻ nhất (4.817 in/3.546 out) nhưng khẳng định SAI authz + bỏ qua docblock; `glm-5.2` đắt ~4,8× input nhưng TỰ đọc `routes/web.php` → authz đúng, dù 3/3 claim XSS sai. Quy trình: verify 100% high/critical + spot-check medium bằng read đúng dòng. Smoke `kimi-k3` (2026-09-07): **6/8 đúng · 1/8 SAI** (không đọc phụ thuộc chéo `store.deleteGen`) · 1/8 thiếu comment chủ đích · **0/5 bẫy "đã fix"** · tìm ra bug mới thật trong code vừa viết (clipboard không await) — nhưng **phồng severity** (2 `[high]` thật là medium/low) → xác minh vẫn bắt buộc.
4. **Số dòng subagent không đáng tin tuyệt đối** — task cấp `offset 1 limit 950` nhưng finding trích :1497/:3186/:4409/:4543 (D); 0/3 claim spot-check đúng nguyên văn. Luôn read lại trước khi ghi.
5. **K.4 — thay thế cơ học N site phải LOẠI TRỪ site nằm trong chính helper mới** (đệ quy vô hạn → SIGKILL cả test suite + 500 production). Test đếm query chống N+1 là CHƯA ĐỦ — phải **assert giá trị**; constraint của `ofMany/latestOfMany` PHẢI truyền qua closure (subquery build tươi không nạp constraint chained).
6. **K.6 — worktree "baseline" symlink `vendor` KHÔNG sạch**: PHP `__DIR__` resolve symlink về tree CHÍNH → class `App\` autoload từ tree chính → bằng chứng "crash cả trên baseline" bị nhiễm. Baseline thật: copy vendor hoặc `composer install` riêng. **Race phiên song song**: commit quét tree (`85b9f91`/`af5a86f`/`f35fd00`/`8817f85`) cuốn việc đang làm của phiên khác + vá nhầm cả file mồ côi (J.5×SourceCard) → phối hợp commit; hai phiên phát hiện độc lập cùng bug có thể fix trùng khớp từng dòng (K.7#4 — tín hiệu tốt).
7. **L.1 — stack frame #47290 trên production log = chữ ký đệ quy vô hạn** (pipeline Laravel thường ≤60 frame) → chẩn đoán được từ 1 dòng log. Sau fix phải deploy + rebuild assets ngay; production chạy code cũ thì mọi fix local vô nghĩa.
8. **Định tuyến provider/model hiện hành**: việc tốn hạn mức (file lớn/phân tích sâu) → `deepseek-official`; việc nhỏ tập trung → `qwen-token-plan` (DashScope compatible-mode, `QWEN_TOKEN_PLAN_API_KEY`, rẻ hơn — cùng `deepseek-v4-pro`); chỉ dùng `glm-5.2` khi task thật sự cần suy luận liên file; **frontend chiều sâu (Vue lifecycle/listener/race/a11y) → `kimi-k3`** (smoke đo 2026-09-07: 6/8 đúng · 0/5 bẫy · tìm ra bug mới trong code vừa viết · phồng severity · 3 step/24.772 in/6.280 out — chi tiết + bằng chứng từng claim: `DELEGATION_PLAYBOOK.md` §1). Điều phối tự review trực tiếp các phạm vi còn lại (lượt 3: 4/4 area).

## 7. Ánh xạ phần cũ → vị trí mới

| Phần cũ (995 dòng) | Nội dung còn giá trị nằm ở |
|---|---|
| Tổng kết + Top-5 vấn đề + delegation log | §0 · §1 (đã đóng) · §2 (top-5 #1-#5) · §3 (high/critical còn lại) · §6#1-2,8 |
| A — workflow lượt 1 (6 area · 70 findings) | Project 11/11 → §2 (K vá hết); ImageAIService/Product/store.js/TryOn/Library → §3#1-5,7-11 + §5 |
| B — lượt 2 (config/models + Vue settings · 20) | §3#9 · §5.1 (cụm config/models, SwapCard/SuggestCard) · §5.2 |
| C — điều phối tự review (3 chunk Controller + 5 Vue lớn · 23) | top-5 #1/#4/#5 → §2; còn lại (download/palette, GD silent, RefImageCard v-html, thumb DoS...) → §5.1 + §5.2 |
| D — kiểm chứng playbook (2 medium + 1 loại) | 2 medium → §5.1 (swapModel, reconcile) · claim loại → §4 · bài học → §6#4 |
| E — smoke-test 2 model (4 findings + 1 loại) | getMessage → §2 (J.4) · race + ensureTables → §5.1 (gộp F) · claim loại → §4 · so sánh model → §6#3 |
| F — blade nhỏ + StylistCatalog (2 low + 4 loại) | savePreset/ensureTables → §5.1 (gộp E) · 4 claim loại → §4 · bảng model → §6#3,8 |
| G — đính chính báo cáo | env() loại → §4 · đếm lại 41 → §2 (J.3) · bảng 4 pattern → §2 (J.2-J.6 đã vá cả 4) |
| H — 3 blade lớn (42 findings) | H.0 → §4 · H.1 xóa file → §2 · H.2-H.4 (24) → VÔ HIỆU · H.5-H.6 settings blade → §5.2 + §5.3 (xác nhận âm tính) · H.7 library blade → §5.2 + §5.3 |
| I — creative-dir + QUEUE B (46 findings) | I.1 orphan → T5 · M6/M8 → T4 · cross-note top-5 → §2 · I.2-I.7 còn lại → §5 · I.8 loại → §4 |
| J — đợt vá phase 2 | Toàn bộ → §2 (J.2-J.6) + §1 "đã đóng" (J.1, J.8#4-5) |
| K — hoàn thiện Project (11 fix + 2 bug mới + UX + K.8) | K.1-K.5 → §2 · K.4 bài học → §6#5 · K.6 race → §6#6 · K.7 → §1 (T3/T4/T7 + đã đóng) · K.8 → §2 + T6 |
| L — production-refgen | L.1 → **T1** + §6#7 · L.2 → §2 · L.3 → §0 baseline |
| **M — đồng bộ Project × Outputs/Library + áp dụng dự án + icon** (phiên sau L, điều phối 4 subagent: A LibraryApp · B GalleryModal · C ProjectWorkspace · D OutputModule+StudioApp; 3/4 fail giữa chừng do provider → resume thành công) | Toàn bộ → §2 (hàng M) · T6 nửa đầu → §1 · baseline mới → §0 · việc còn lại (move 1-chạm, pattern/tryon gửi project_id) → §1 T6 + §2 hàng M "giới hạn" |
